name: Nightly MCP Sync

on:
  schedule:
    - cron: '0 2 * * *'   # 2:00am UTC every night
  workflow_dispatch:        # also allows manual trigger from GitHub Actions tab

jobs:
  sync:
    name: Sync MCP metadata from GitHub
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run sync
        run: npm run sync
        env:
          # Use a PAT (5,000 req/hour), NOT the default GITHUB_TOKEN (1,000/hour).
          # The sync makes one API call per MCP, which exceeds the default limit.
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
